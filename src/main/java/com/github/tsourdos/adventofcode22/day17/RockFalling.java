package com.github.tsourdos.adventofcode22.day17;

public class RockFalling {
    private String in;

    public void analyse(String input) {
        in = input;
        int inIndex = 0;
        char[][] array = new char[5000][7];
        for (int i = 0; i < 2021; i++) {
            boolean bottomHit = false;
            while (!bottomHit){

            }
            if (inIndex >= input.length()) {
                inIndex = 0;
            }
            char c = input.charAt(inIndex);

            inIndex++;
        }
    }
}
